---
cluster:
  name: "docker-cluster"
network:
  host: 0.0.0.0
node:
  name: ${NODE_NAME}
discovery:
  type: ${DISCOVERY_TYPE}
path:
  data: /var/lib/elasticsearch-data
  logs: /var/log/elasticsearch
  repo: /usr/share/elasticsearch/snapshot
xpack:
  security:
    enabled: false
